/** Player starting gold (after territory is picked). */
export const RTS_STARTING_GOLD = 10;

/** Gold cost per new unit at Town Hall. */
export const RTS_UNIT_COST_GOLD = 2;

/** Gold cost to build a Market. */
export const RTS_MARKET_COST_GOLD = 5;

/** Default: 1 gold per working unit per this many ms of game time. */
export const RTS_DEFAULT_MARKET_INCOME_INTERVAL_MS = 30_000;

export const RTS_STARTING_UNITS = 3;

/** Own this many territories at once to win the 5-territory slice. */
export const RTS_MVP_DOMINATION_TERRITORIES = 3;

export const RTS_SCHEMA_VERSION = 1 as const;
